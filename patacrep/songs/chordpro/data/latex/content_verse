(*- if content.nolyrics -*)
\ifchorded
\begin{verse*}
\musicnote{\nolyrics
  (* for line in content.lines *)
    (( render(line) ))
    (*- if not loop.last *)\newline(* endif *)

  (* endfor *)
}
\end{verse*}
\fi
(*- else *)
\begin{(( content.type ))}
  (* for line in content.lines *)
    (( render(line) ))
  (* endfor *)
\end{(( content.type ))}
(*- endif *)
