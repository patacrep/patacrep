(* if content.directive() *)
  (* for line in content.lines -*)
    ((- render(line) ))
  (* endfor -*)
(*- else -*)
\begin{(( content.type ))}
  (* for line in content.lines *)
    (( render(line) ))
  (* endfor *)
\end{(( content.type ))}
(*- endif *)
